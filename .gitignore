# On-disk relation cache, regenerated from parquet on a cache miss (~9GB).
cache/
# Per-scale-factor caches (e.g. sf=1 regenerated for oracle-matching runs).
cache-sf*/
# TPC-H parquet + the data/cache symlink that `bench/setup_tpch.sh` writes.
data/

# Rust build artifacts
rust/target/
rust/.hegel/
rust/src/test/queries/job/*/mismatch.sql
schema_experiments/target/
schema_sandbox/target/

# Local Haskell tooling and build artifacts
/cabal.project
/hie.yaml
/dist-newstyle/

# Zig build artifacts
zig/zig-out/
zig/.zig-cache/

# Local Lean experiment and build artifacts
/lean/

# Editor / LyX backups
/.vscode/
*~
*.swp
.DS_Store

*.pdf
notes/
sandbox/
.claude/worktrees
rust/bench/__pycache__
\#prela.lyx#/
haskell/dist-newstyle/

# Local editor and generated build output
.vscode/
dist-newstyle/
lean/.lake/
